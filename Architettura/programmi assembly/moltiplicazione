			mov		r1,#7 ; //commentino//  r1 "rappresenta" 7
			mov		r2,#5 ; maledetto 5
			
			mov		r0,#0
			
loop
			cmp		r2,#0 ; "compare" viene comparato r2 con 0, potrà essere < , = o >
			beq		end_program
			
			add		r0,r0,r1
			sub		r2,r2,#1
			b		loop
			
end_program
			end
			
			
